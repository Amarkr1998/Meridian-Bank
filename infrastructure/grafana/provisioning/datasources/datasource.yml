# Meridian Bank — Grafana datasource provisioning (Phase 16).
# Auto-registers Prometheus as a datasource on container startup — no manual UI setup needed.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
