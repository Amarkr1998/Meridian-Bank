# Meridian Digital Banking — frontend local environment variables (example)
#
# Copy to `.env` if you need to override the default. Not required for local development against
# the docker-compose stack — api-gateway listens on localhost:8080 by default (see the root
# .env.example's API_GATEWAY_PORT).

VITE_API_BASE_URL=http://localhost:8080
